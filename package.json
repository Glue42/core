{
  "name": "glue42core",
  "version": "0.0.0",
  "description": "Web interoperability framework",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "npm run test:scripts && lerna run test",
    "version:full": "node ./scripts/preversion/index.js && lerna version",
    "publish:full": "lerna publish from-git --yes",
    "test:scripts": "mocha ./scripts/test/**/*spec.js"
  },
  "devDependencies": {
    "@lerna/changed": "^3.20.0",
    "@lerna/run": "^3.20.0",
    "chai": "^4.2.0",
    "lerna": "^3.20.2",
    "lerna-changelog": "^1.0.0",
    "mocha": "^7.1.0",
    "mockery": "^2.1.0",
    "simple-git": "^1.131.0"
  }
}

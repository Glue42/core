{
  "name": "glue42core",
  "version": "0.0.0",
  "description": "Web interoperability framework",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "version:full": "node ./scripts/preversion.js && lerna version",
    "publish:full": "lerna publish from-git --yes"
  },
  "devDependencies": {
    "@lerna/changed": "^3.20.0",
    "@lerna/run": "^3.20.0",
    "lerna": "^3.20.2",
    "lerna-changelog": "^1.0.0",
    "simple-git": "^1.131.0"
  }
}
